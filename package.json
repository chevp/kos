{
  "name": "@kosmos/kos",
  "version": "0.1.0",
  "description": "Kosmos CLI — local client and dev-stack orchestrator for kos-services, frost-hub, GCP Cloud Run and Firebase Hosting",
  "type": "module",
  "bin": {
    "kos": "./bin/kos"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}